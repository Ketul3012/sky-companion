/**
 * Renders the NoMatch component, which displays a message indicating that no match was found.
 * This component is important to the project as it provides a fallback route for any invalid URLs.
 */
export const NoMatch = () => {
  return (
    <div>
      <h1>No match found</h1>
    </div>
  );
};
